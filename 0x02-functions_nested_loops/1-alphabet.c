#include "holberton.h"
/**
 * main - Entry point
 * print the alphabet in lowercase.
 * Return: Always 0.
 */
int main(void)
{
	char ch;
	for (ch = 'a'; ch <= 'z'; ch++)
	{
		_putchar(ch);
	}
	_putchar('\n');
return (0);
}
