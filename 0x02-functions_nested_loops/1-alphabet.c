#include "holberton.h"
/**
 * main - Entry point
 * print the alphabet in lowercase.
 * Return: Always 0.
 */
int main(void)
{
	for (i = 'a'; i <= 'z'; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
return (0);
}
